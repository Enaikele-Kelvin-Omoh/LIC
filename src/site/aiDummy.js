// const courseContent =

export const generateCourseContent = () => {
  return new Promise(async (resolve, reject) => {
    try {
    } catch (error) {
      console.error(error);
      reject(error);
    }
  });
};
